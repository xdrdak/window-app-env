{
  "name": "window-app-env",
  "version": "0.1.0",
  "description": "",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsup src --format esm,cjs --dts",
    "clean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Xavier Drdak",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "tsup": "^4.12.0",
    "typescript": "^4.3.5"
  }
}
